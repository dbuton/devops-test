apiVersion: v1
kind: Service
metadata:
  name: test-service
  labels:
    name: test-service
spec:
  type: NodePort
  ports:
    - port: 80
      nodePort: 30080
      targetPort: 3000
      name: http
  selector:
    app: test-deployment